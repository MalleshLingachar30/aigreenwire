import { NextResponse } from "next/server";

export async function POST() {
  // TODO: Stage 2 — subscription logic
  return NextResponse.json({ message: "Subscribe endpoint placeholder" }, { status: 200 });
}
